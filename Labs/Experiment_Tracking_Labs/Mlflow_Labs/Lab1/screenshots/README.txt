Add your MLflow UI screenshots here after running the lab.

Suggested screenshots:
1. experiment_runs.png            — Full runs table showing all 9 runs with metrics
2. run_detail_rf.png              — Single Random Forest run detail (params + metrics)
3. confusion_matrix_artifact.png  — Artifact tab showing the confusion matrix plot
4. metric_comparison.png          — MLflow compare view (ROC-AUC across runs)
